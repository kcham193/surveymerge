% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odk_merge.R
\name{odk_merge}
\alias{odk_merge}
\title{Merge an ODK or KoboToolbox export into a flat dataset}
\usage{
odk_merge(
  file_path,
  drop_internal = TRUE,
  col_types = NULL,
  suffix = c("_repeat", "_parent"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{file_path}{A length-1 character string giving the path to the \code{.xlsx}
file exported from ODK or KoboToolbox.}

\item{drop_internal}{Logical. If \code{TRUE} (the default), columns starting with
\verb{_submission_} are removed from repeat sheets before merging.}

\item{col_types}{A character vector passed to \code{\link[readxl:read_excel]{readxl::read_excel()}} to
control column-type parsing. \code{NULL} (default) lets \code{readxl} guess.}

\item{suffix}{A length-2 character vector of suffixes appended to
disambiguate column names that appear in more than one sheet.
Default: \code{c("_repeat", "_parent")}.}

\item{verbose}{Logical. If \code{FALSE}, all informational messages are
suppressed. Default: \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[readxl:read_excel]{readxl::read_excel()}}.}
}
\value{
\itemize{
\item A single tibble when the file contains exactly one repeat sheet.
\item A named list of tibbles when the file contains multiple repeat sheets
(one tibble per repeat group).
}
}
\description{
The main entry point for \code{odkmerge}. Reads an \code{.xlsx} export, detects the
repeat-group structure, and returns a flat, analysis-ready dataset — all in
one call.

Internally this function runs:
\code{\link[=read_odk_export]{read_odk_export()}} → \code{\link[=detect_structure]{detect_structure()}} → \code{\link[=build_master]{build_master()}}.

For more control over any step, call those functions individually.
}
\examples{
path <- system.file("extdata", "simple_survey.xlsx", package = "odkmerge")
master <- odk_merge(path)
head(master)

}
