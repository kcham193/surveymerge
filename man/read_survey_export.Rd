% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_survey.R
\name{read_survey_export}
\alias{read_survey_export}
\title{Read a relational survey export (XLSForm \code{.xlsx})}
\usage{
read_survey_export(file_path, col_types = NULL, ...)
}
\arguments{
\item{file_path}{A length-1 character string giving the path to the
\code{.xlsx} file.}

\item{col_types}{A character vector passed to \code{\link[readxl:read_excel]{readxl::read_excel()}} to
control column-type parsing. Applied identically to every sheet. Set
\code{NULL} (the default) to let \code{readxl} guess types automatically.}

\item{...}{Additional arguments passed to \code{\link[readxl:read_excel]{readxl::read_excel()}} for each
sheet (e.g. \code{na}, \code{trim_ws}).}
}
\value{
A named list of tibbles, one per sheet. Names match the sheet names in
the source file. Each tibble preserves the original columns, including
the \verb{_index} / \verb{_parent_index} (KoboToolbox) or \code{KEY} / \code{PARENT_KEY}
(ODK Central) columns that downstream functions use to reconstruct
parent-child relationships.
}
\description{
Reads every sheet from an \code{.xlsx} export produced by an XLSForm-based
survey platform (ODK Central, KoboToolbox, or any tool that follows the
same conventions) and returns them as a named list of tibbles. This list
is the entry point for all the other \code{surveymerge} functions.

The function does \strong{not} make any decision about how the sheets relate
to one another or which one is your unit of analysis - that is what
\code{\link[=detect_structure]{detect_structure()}} and \code{\link[=build_master]{build_master()}} are for. It simply gives you
faithful, per-sheet access to the export.
}
\section{What this means for analysis}{

The returned list represents the raw relational structure of your
survey. Each sheet has its own grain (one row per household, one row
per member, one row per visit, ...). Treat it as a \emph{family of related
tables}, not a single dataset.
}

\examples{
path   <- system.file("extdata", "simple_survey.xlsx", package = "surveymerge")
sheets <- read_survey_export(path)
names(sheets)   # "survey" "species"

}
