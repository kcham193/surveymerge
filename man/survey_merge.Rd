% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_merge.R
\name{survey_merge}
\alias{survey_merge}
\title{Read and assemble analysis-ready datasets from a survey export}
\usage{
survey_merge(
  file_path,
  drop_internal = TRUE,
  col_types = NULL,
  suffix = c("_repeat", "_parent"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{file_path}{A length-1 character string giving the path to the
\code{.xlsx} file exported from ODK Central, KoboToolbox, or another
XLSForm-based survey platform.}

\item{drop_internal}{Logical. If \code{TRUE} (the default), columns starting
with \verb{_submission_} (KoboToolbox) and ODK Central's system columns
are removed before merging.}

\item{col_types}{A character vector passed to \code{\link[readxl:read_excel]{readxl::read_excel()}} to
control column-type parsing. \code{NULL} (default) lets \code{readxl} guess.}

\item{suffix}{A length-2 character vector of suffixes appended to
disambiguate column names that appear in more than one sheet.
Default: \code{c("_repeat", "_parent")}.}

\item{verbose}{Logical. If \code{FALSE}, all informational messages are
suppressed. Default: \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[readxl:read_excel]{readxl::read_excel()}}.}
}
\value{
\itemize{
\item A single tibble when the file contains exactly one repeat sheet
(one row per repeat record, with parent columns attached).
\item A named list of tibbles when the file contains multiple repeat sheets
\itemize{
\item \strong{one tibble per unit of analysis}. Sibling repeats live at
different grains, so they are kept as separate tibbles rather than
blindly stacked together.
}
}
}
\description{
The one-call wrapper for \code{surveymerge}. Reads an \code{.xlsx} export, detects
the parent-child sheet structure, and returns a dataset (or one dataset
per repeat group) at the appropriate \strong{unit of analysis}.

Internally this function runs:
\code{\link[=read_survey_export]{read_survey_export()}} -> \code{\link[=detect_structure]{detect_structure()}} -> \code{\link[=build_master]{build_master()}}.

For more control over any step - especially the choice of which repeat
grain to use and which parent columns to carry across - call those
functions individually, or use \code{\link[=enrich_repeat]{enrich_repeat()}}.
}
\section{What this means for analysis}{

The output is shaped by the \emph{structure} of your survey, not by an
assumption about your analysis. Before using the result, pick the
tibble whose grain matches your research question:
\itemize{
\item Household-level descriptive statistics -> use the parent sheet
directly (\code{read_survey_export(path)[[structure$parent_sheet]]}) or
\code{\link[=enrich_repeat]{enrich_repeat()}} with parent-level aggregates.
\item Individual / member-level analysis -> use the \code{members} tibble.
\item Visit / event-level analysis -> use the corresponding leaf-grain
tibble.
}

Pooling tibbles across grains inflates record counts; the package
deliberately does not do this for you.
}

\examples{
path   <- system.file("extdata", "simple_survey.xlsx", package = "surveymerge")
master <- survey_merge(path)
head(master)

}
