# NA

## What does this PR do?

A short summary of the change.

Fixes \#

## Type of change

Bug fix

New feature

Breaking change

Documentation only

## Checklist

I have updated `NEWS.md` (if user-facing).

I have added tests under `tests/testthat/`.

`devtools::document()` has been re-run if roxygen tags changed.

`devtools::check()` passes locally with 0 errors, 0 warnings, 0 notes.

Any new exported function has examples and `@return` documentation.
